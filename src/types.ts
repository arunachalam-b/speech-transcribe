import { RENDERER_ROUTE, RENDERER_ROUTE_ACTION } from './constants';

type UpdateRendererRouteArgs = {
  action: RENDERER_ROUTE_ACTION;
  route: RENDERER_ROUTE;
};

export type { UpdateRendererRouteArgs };
